{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
<main class="contact">
 <img src={{ asset('assets/images/background/desk-bandeau-contact.jpg') }} alt="bandeau Contact" width="100%">

<div class="container h-100">
<h1 class="mt-5 mb-3 font-weight-bold text-center ms-auto me-auto">Vous avez des questions ? Vous souhaitez parler <span class="yellow">à nos conseillers pédagogiques ?</span>
</h1>
<p class="text-left">
Que ce soit pour un projet professionnel, une reconvertion, une évolution ou pour monter en compétences.
</p>
<p class="text-left">
Nos conseillers pédagogiques sont à votre disposition pour vous informer et vous accompagner dans l'élaboration de votre projet ou pour vous aider à monter votre dossier de financement.
</p>
<h2 class="h3 mb-3 font-weight-bold text-center">
<span class="yellow">Contactez-nous</span><br>
par message ou par téléphone<br>
au <span class="yellow">01.00.00.00.00</span></h2>


        <div class="row h-100 justify-content-center align-items-center">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success" role="alert">
                    {{ message }}
                </div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="alert alert-danger" role="alert">
                    {{ message }}
                </div>
            {% endfor %}
        </div>
        <div class="row h-100 justify-content-center align-items-center p-5">
            
                
               {# {{ form(formulaire) }} #}
 <div class="form-contact">
 {{ form_start(formulaire) }} 
                <div class="row mb-3">
                    <div class="col me-3">
                       {{ form_widget(formulaire.lastName) }}
                    </div>
                    <div class="col me-3">
                       {{ form_widget(formulaire.firstName) }}
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col me-3">
                       {{ form_widget(formulaire.email) }}
                    </div>
                    <div class="col me-3">
                      {{ form_widget(formulaire.phone) }}
                    </div>
                </div>



<div class="row mb-3 form-background align-item-center">
 <div class="col-3">{{ form_label(formulaire.public) }}</div>
 <div class="col-9">
   {{ form_widget(formulaire.public) }}
  </div>
</div>

<div class="row mb-3">
    <div class="col me-3">
        {{ form_widget(formulaire.message) }}
    </div>
</div>
                <div class="form-group mb-3">
                     <div class="form-check d-flex align-items-center">
                       <input class="form-check-input" type="checkbox" value="" id="cgu" required>
                       <label class="form-check-label">Conformément à notre <a href="{{ path('protection') }}"><span class="yellow">politique de confidentialité</span></a>, nous nous engageons à respecter vos données personnelles.</label>
                       
                     </div>
                 </div>
            
           {{ form_end(formulaire) }}
           </div>
            </div>
       
    </div>
    </main>
{% endblock %}
