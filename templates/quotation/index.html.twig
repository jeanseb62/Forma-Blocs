{% extends 'base.html.twig' %}

{% block metaDescription %}Devis gratuit en ligne. Nous nous engageons à vous répondre sous 24h. {% endblock metaDescription %}
{% block robots %}index,follow{% endblock robots %}
{% block title %}Demander un devis - {{ parent() }}{% endblock %}


{% block body %}
<main class="devis">
<img src={{ asset('assets/images/background/desk-bandeau-devis.jpg') }} alt="bandeau devis" width="100%">
<section class="container mt-5">

<h1 class="mb-3 font-weight-bold text-center ms-auto me-auto">Vous souhaitez un devis sur une des prestations de <span class="yellow"> FormaBLOCS</span> ?
</h1>

<p class="text-left">
Merci de remplir le formulaire ci-dessous.
</p>

        <div class="row h-100 justify-content-center align-items-center">
            {% for message in app.flashes('message') %}
                <div class="alert alert-success" role="alert">
                    {{ message }}
                </div>
            {% endfor %}
        </div>
{{ form_start(quotation) }} 
    <div class="form-devis h-100 justify-content-center align-items-center mb-3">
            
  
                <div class="row">
                    <div class="col-sm-12 col-md-6 mb-3">
                      {{ form_widget(quotation.lastName) }}
                     
                    </div>
                    <div class="col-sm-12 col-md-6 mb-3">
                   
                      {{ form_widget(quotation.firstName) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-3 col-md-3 mb-3 numberStreet">
                      {{ form_widget(quotation.numberStreet) }}
                    </div>
                    <div class="col-sm-9 col-md-9 mb-3 street">
                      {{ form_widget(quotation.street) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-sm-12 col-md-3 mb-3">
                      {{ form_widget(quotation.zip) }}
                    </div>
                    <div class="col-sm-12 col-md-9 mb-3">
                      {{ form_widget(quotation.city) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-3 mb-3">
                      {{ form_widget(quotation.country) }}
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-sm-12 col-md-6 mb-3">
                      {{ form_widget(quotation.email) }}
                    </div>
                    <div class="col-sm-12 col-md-6">
                      {{ form_widget(quotation.phone) }}
                    </div>
                </div>
                
<div class="row align-items-center form-background">
 <div class="col-sm-12 col-md-2">{{ form_label(quotation.status) }}</div>
 <div class="col-sm-12 col-md-10 status">
  {{ form_widget(quotation.status) }}
  </div>
</div>


<div class="rowd-flex form-background">
 <div class="col-sm-12 col-md-2">{{ form_label(quotation.benefit) }}</div>
 <div class="col-sm-12 col-md-10 prestations">
 <label>
  {{ form_widget(quotation.benefit) }}
</label>   
</div>
</div>

<div class="devisMobile"> 
<p>*veuillez noter dans le message le bloc de formation souhaité.<br>
Merci 
</p>
</div> 

<div class="row mb-3">
    <div class="col">
        {{ form_widget(quotation.message) }}
    </div>
</div>

<div class="row align-items-center form-background">
 <div class="col-sm-12 col-md-3 mb-3">{{ form_label(quotation.SendByEmail) }}</div>
 <div class="col-sm-12 col-md-9 mb-3">
  {{ form_widget(quotation.SendByEmail) }}
  </div>
</div>
              <div class="devisDesktop">  
            <button class="btn-yellow mt-3" type="submit">
        ENVOYER
    </button></div>
  
           
            
            </div>
                <div class="devisTexte">
                       <span><input class="form-check-input" type="checkbox" value="" required>
                      <label>Conformément à notre <a href="{{ path('protection') }}"><span class="yellow">politique de confidentialité</span></a>, nous nous engageons à respecter vos données personnelles.</label></span>
<div class="devisMobile">  
            <button class="btn-yellow mt-3" type="submit">
        ENVOYER
    </button></div>
                 <p class="mb-4">L’équipe de <span class="yellow">FormaBLOCS</span> reprendra contact avec vous dans les 24/48h.</p>
<div class="devisDesktop"> 
<p>*Pour les blocs individuels de formation, notez dans le message le bloc que vous souhaitez valider. </p>
</div> 
     </div> 
     {{ form_end(quotation) }}
    
    </div>
   
</section>
</main>
{% endblock %}

